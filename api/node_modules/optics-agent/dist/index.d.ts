import Agent from './Agent';
import { configureAgent, instrumentSchema, middleware, instrumentHapiServer, context } from './DefaultAgent';
declare var _default: {
    configureAgent: (options: any) => void;
    instrumentSchema: (schema: any) => any;
    middleware: () => any;
    instrumentHapiServer: (server: any) => any;
    context: (req: any) => any;
    Agent: typeof Agent;
};
export default _default;
export { configureAgent, instrumentSchema, middleware, instrumentHapiServer, context, Agent };
